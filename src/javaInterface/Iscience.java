package javaInterface;

public interface Iscience {
    void scienceTeacher();
//by default all methods in interface must contain declaration part only NOT Definition.
//& it must always be public access modifier neither or nor we mentioned it.
}
